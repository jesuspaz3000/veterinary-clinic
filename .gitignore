# Local Claude Code state (machine-specific permissions/history, not shared config)
.claude/settings.local.json
.claude/*.local.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Docker env (secretos reales para docker-compose, no subir a git)
.env
