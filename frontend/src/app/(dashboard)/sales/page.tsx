import SalesFeature from "@/features/sales";

export const metadata = {
  title: "Ventas y Facturación (POS) - VetGestión",
  description: "Punto de Venta, facturación, boletas y kardex de inventario",
};

export default function SalesPage() {
  return <SalesFeature />;
}
